class EditArticle {
  /**
   * @param {srting} slug
   * @param {*} params
   */
  constructor(
    slug,
    params,
  ) {
    this.slug = slug;
    this.params = params;
  }
}

module.exports = EditArticle;
